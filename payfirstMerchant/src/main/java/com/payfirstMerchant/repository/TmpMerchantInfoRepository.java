package com.payfirstMerchant.repository;

import com.payfirstMerchant.model.TmpMerchantInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TmpMerchantInfoRepository extends JpaRepository<TmpMerchantInfo, Long> {
}
